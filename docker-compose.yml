services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./app/calorieHistory.csv:/app/calorieHistory.csv     # persist CSV
      - ./app:/app:ro    